                  if (Type->Qualifier & TypeQual_None)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_None ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_ThreadLocal)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_ThreadLocal ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Const)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Const ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Static)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Static ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Volatile)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Volatile ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Unsigned)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Unsigned ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Signed)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Signed ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Int)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Int ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Char)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Char ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Long)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Long ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_LongLong)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_LongLong ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_LongDouble)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_LongDouble ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Short)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Short ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Struct)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Struct ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Union)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Union ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Enum)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Enum ")); // TODO(Jesse): Strip prefix and lowercase
          }
          if (Type->Qualifier & TypeQual_Inline)
          {
            // Append(&Builder, CSz("(EnumVal.name > strip_prefix > lowercase) ")); // TODO(Jesse): Strip prefix and lowercase
            Append(&Builder, CSz("TypeQual_Inline ")); // TODO(Jesse): Strip prefix and lowercase
          }


